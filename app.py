# -------------------------
# 匯入需要的套件
# -------------------------
import streamlit as st              # Streamlit:用來做網頁介面
import pandas as pd                 # pandas:讀取與處理 Excel 資料
import random                       # random:隨機選號、幸運值
from collections import Counter     # Counter:統計出現次數
from itertools import combinations  # combinations:計算號碼共現關係
from datetime import datetime, timedelta  # datetime:處理日期篩選

# =====================================================
# 模型預設參數(整個 App 的基準設定)
# =====================================================

DEFAULT_FREQ_WEIGHT = 0.6     # 歷史出現頻率影響程度
DEFAULT_CO_WEIGHT = 0.2       # 號碼共現關係影響程度
DEFAULT_NOISE = 0.5           # 隨機擾動強度(玄學成分)

# 隨機擾動實際使用的範圍
DEFAULT_NOISE_RANGE = (1 - DEFAULT_NOISE, 1 + DEFAULT_NOISE)


# -------------------------
# 設定網頁基本資訊
# -------------------------
st.set_page_config(
    page_title="樂透智慧選號器",
    page_icon="🎯",
    layout="centered"
)

# =====================================================
# 初始化 Session State(只在第一次載入時執行)
# =====================================================

if "freq_w" not in st.session_state:
    st.session_state.freq_w = DEFAULT_FREQ_WEIGHT

if "co_w" not in st.session_state:
    st.session_state.co_w = DEFAULT_CO_WEIGHT

if "noise" not in st.session_state:
    st.session_state.noise = DEFAULT_NOISE

if "years_filter" not in st.session_state:
    st.session_state.years_filter = 3  # 預設使用近3年資料

# -------------------------
# 網頁標題與說明
# -------------------------
st.title("🎯 樂透智慧選號器")
st.caption("統計理工 × 天選之人｜理性與命運的交會 🤞")

# =====================================================
# Step 1:選擇樂透種類
# =====================================================
game_type = st.radio(
    "🎮 選擇樂透玩法",
    ["大樂透", "威力彩"],
    horizontal=True
)

# =====================================================
# Step 2:選擇選號模式
# =====================================================
mode = st.radio(
    "🎛️ 選號模式",
    ["統計理工模式 🧠", "天選之人模式 🔮"]
)

# =====================================================
# Step 3:依玩法讀取對應資料
# =====================================================
try:
    if game_type == "大樂透":
        # 讀取大樂透歷史資料
        df = pd.read_excel("lotto_big.xlsx")
        
        # 六個主號欄位名稱
        number_cols = ["獎號1", "獎號2", "獎號3", "獎號4", "獎號5", "獎號6"]
        
        # 主號範圍 1~49
        number_range = range(1, 50)
        
        # 大樂透沒有第二區
        special_range = None
    
    else:
        # 讀取威力彩歷史資料
        df = pd.read_excel("lotto_power.xlsx")
        
        # 六個第一區號碼欄位
        number_cols = ["獎號1", "獎號2", "獎號3", "獎號4", "獎號5", "獎號6"]
        
        # 第一區號碼範圍 1~38
        number_range = range(1, 39)
        
        # 第二區號碼範圍 1~8
        special_range = range(1, 9)
    
    # 儲存原始完整資料
    df_full = df.copy()

except FileNotFoundError:
    st.error("❌ 找不到歷史資料檔案,請確認 lotto_big.xlsx 和 lotto_power.xlsx 是否存在")
    st.stop()
except Exception as e:
    st.error(f"❌ 讀取資料時發生錯誤: {str(e)}")
    st.stop()

# =====================================================
# Step 4:統計理工模式的進階設定
# =====================================================
if mode == "統計理工模式 🧠":
    st.markdown("### ⚙️ 模型參數設定")
    
    # 資料期數篩選
    years_option = st.selectbox(
        "📅 使用歷史資料期數",
        options=[1, 3, 5, "全部"],
        index=1,  # 預設選「近3年」
        format_func=lambda x: f"近 {x} 年" if isinstance(x, int) else "全部資料(15年)",
        help=(
            "選擇要參考的歷史開獎期數:\n\n"
            "• 近1年: 最新趨勢,樣本較少\n"
            "• 近3年: 推薦選項,平衡時效性與穩定性\n"
            "• 近5年: 更多樣本,統計更穩定\n"
            "• 全部資料: 包含所有歷史記錄"
        )
    )
    
    # 根據選擇篩選資料
    if years_option != "全部":
        # 使用「期別」欄位篩選(格式: YYYXXXXX, 例如 114000086 或 097000001)
        if "期別" in df_full.columns:
            # 將期別轉為字串,並補足到9位數(前面補0)
            df_full["期別_str"] = df_full["期別"].astype(str).str.zfill(9)
            
            # 取前3碼當作民國年
            df_full["年份"] = df_full["期別_str"].str[:3].astype(int)
            
            # 取得最新一期的年份
            latest_year = df_full["年份"].max()
            
            # 計算要篩選的起始年份(例如最新是114年,要近3年就是111年起)
            cutoff_year = latest_year - years_option
            
            # 篩選大於等於起始年份的資料
            df = df_full[df_full["年份"] >= cutoff_year].copy()
            
            # 取得實際使用的年份範圍(用於顯示)
            actual_start_year = df["年份"].min()
            actual_end_year = df["年份"].max()
            year_range_text = f"{actual_start_year}-{actual_end_year}年"
            
            # 移除暫時欄位
            df = df.drop(columns=["年份", "期別_str"])
        else:
            # 如果沒有期別欄位,就按比例保留最近的資料(備用方案)
            total_rows = len(df_full)
            keep_rows = int(total_rows * (years_option / 15))
            df = df_full.tail(keep_rows).copy()
            year_range_text = f"近{years_option}年"
    else:
        df = df_full.copy()
        year_range_text = "全部年份"
    
    # 只留下六個主號,方便後續計算
    numbers_df = df[number_cols]
    
    # 顯示使用的資料期數和年份範圍
    st.info(f"📊 使用 **{year_range_text}** 開獎資料進行分析 (共 **{len(numbers_df)}** 期)")
    
    freq_w = st.slider(
        "📊 歷史頻率權重",
        0.0, 1.0,
        step=0.05,
        key="freq_w",
        help=(
            "📈 控制『歷史常出現號碼』的影響力。\n\n"
            "數值越高,越偏好過去出現次數多的號碼;\n"
            "數值越低,歷史資料影響越小。"
        )
    )
    
    co_w = st.slider(
        "🔗 號碼配對權重",
        0.0, 1.0,
        step=0.05,
        key="co_w",
        help=(
            "🔗 控制『號碼彼此一起出現』的影響力。\n\n"
            "這是統計學中的「共現分析」,分析哪些號碼在歷史上常一起開出。\n"
            "數值越高,越偏好歷史上常一起出現的號碼組合;\n"
            "數值越低,組合關係影響越小。"
        )
    )
    
    noise = st.slider(
        "🎲 隨機擾動強度",
        0.0, 1.0,
        step=0.05,
        key="noise",
        help=(
            "🎲 控制『隨機性/變化度』的強度。\n\n"
            "數值越高,每次結果變化越大;\n"
            "數值越低,選號結果越穩定。"
        )
    )

else:
    # 天選之人模式使用全部資料(雖然不影響結果,但保持一致性)
    df = df_full.copy()
    numbers_df = df[number_cols]

# =====================================================
# Step 5:統計歷史出現頻率(只在理工模式使用)
# =====================================================
freq_counter = Counter(numbers_df.values.flatten())  # 每個號碼出現次數
max_freq = max(freq_counter.values()) if freq_counter else 1  # 最大出現次數(用來正規化)

# =====================================================
# Step 6:計算號碼共現關係(兩兩一起出現)
# =====================================================
pair_counter = Counter()

for row in numbers_df.values:
    # 每一期的 6 個號碼,取所有兩兩組合
    for a, b in combinations(sorted(row), 2):
        pair_counter[(a, b)] += 1

# 共現次數最大值(避免除以 0)
max_pair = max(pair_counter.values()) if pair_counter else 1

# =====================================================
# Step 7:統計理工模式 → 建立權重
# =====================================================
def build_weights(freq_w, co_w, noise_range):
    """
    建立每個號碼的權重(越大越容易被抽中)
    
    參數:
        freq_w: 歷史頻率權重係數
        co_w: 共現關係權重係數
        noise_range: 隨機擾動範圍(最小值, 最大值)
    
    回傳:
        weights: 字典,key是號碼,value是該號碼的權重
    """
    weights = {}
    
    for num in number_range:
        # 歷史頻率權重(正規化到 0~1)
        freq_score = freq_counter.get(num, 0) / max_freq
        
        # 與其他號碼的共現程度(正規化到 0~1)
        co_score = sum(
            pair_counter.get((min(num, other), max(num, other)), 0)
            for other in number_range if other != num  # 排除自己
        ) / max_pair
        
        # 隨機擾動(玄學來源)
        noise = random.uniform(*noise_range)
        
        # 最終權重 = (頻率分數 × 頻率係數 + 共現分數 × 共現係數) × 隨機擾動
        raw_weight = (freq_w * freq_score + co_w * co_score)
        weights[num] = max(raw_weight * noise, 1e-6)  # 確保最小值不為0
    
    return weights

# =====================================================
# Step 8:依權重產生 6 個不重複號碼(統計理工模式)
# =====================================================
def generate_weighted_numbers(weights):
    """
    根據權重抽出6個不重複的號碼
    
    參數:
        weights: 每個號碼的權重字典
    
    回傳:
        selected: 排序後的6個號碼清單
    """
    available = list(weights.keys())  # 所有可選號碼
    selected = []  # 已選中的號碼
    
    total_weight = sum(weights.values())
    if total_weight <= 0:
        # 極端情況退回純隨機
        return sorted(random.sample(available, 6))
    
    # 依序抽出6個號碼,每抽一個就從候選池中移除
    for _ in range(6):
        chosen = random.choices(
            available,
            weights=[weights[n] for n in available],  # 只計算剩餘號碼的權重
            k=1
        )[0]
        
        selected.append(chosen)
        available.remove(chosen)  # 移除已選號碼,確保不重複
    
    return sorted(selected)  # 回傳排序後的號碼

# =====================================================
# Step 9:完全隨機選號(天選之人模式)
# =====================================================
def generate_random_numbers():
    """
    完全隨機選出6個不重複號碼(不考慮歷史資料)
    
    回傳:
        排序後的6個號碼清單
    """
    return sorted(random.sample(list(number_range), 6))

# =====================================================
# Step 10:幸運指數計算 - 統一改為隨機 🎲
# =====================================================
def calculate_luck_index():
    """
    計算本組號碼的幸運指數
    
    兩種模式都使用隨機生成,純粹的運氣成分!
    
    回傳:
        65-98之間的整數幸運指數
    """
    # 統一使用隨機生成,範圍在65-98之間
    # 故意設定偏高,讓使用者有「幸運」的感覺
    return random.randint(65, 98)

# =====================================================
# Step 11:按鈕 → 產生建議號碼
# =====================================================
if st.button("🎰 產生建議號碼", type="primary"):
    
    # 根據模式選擇對應的選號方式
    if mode == "統計理工模式 🧠":
        # 統計模式:建立權重後依權重選號
        weights = build_weights(
            st.session_state.freq_w,
            st.session_state.co_w,
            (
                1 - st.session_state.noise,
                1 + st.session_state.noise
            )
        )
        main_nums = generate_weighted_numbers(weights)
    
    else:
        # 天選模式:完全隨機選號
        main_nums = generate_random_numbers()
    
    # 計算幸運指數(兩種模式都是隨機)
    luck = calculate_luck_index()
    
    # 格式化號碼顯示(補零到兩位數,用頓號分隔)
    formatted = "、".join(f"{n:02d}" for n in main_nums)
    
    st.subheader("🎯 建議號碼")
    
    # 顯示第一區號碼
    if game_type == "威力彩":
        st.success(f"第一區: {formatted}")
    else:
        st.success(formatted)
    
    # 威力彩需要額外顯示第二區號碼
    if game_type == "威力彩":
        special = random.choice(list(special_range))  # 第二區隨機選1個
        st.info(f"第二區: {special:02d}")
    
    # 顯示幸運指數
    st.markdown(f"### ✨ 本組幸運指數: **{luck}**")
    
    # 根據幸運指數給予不同的鼓勵訊息
    if luck >= 90:
        st.success("🌟 超級幸運組合!財神爺都在對你微笑!")
    elif luck >= 80:
        st.success("✨ 幸運爆棚!今天就是你的大日子!")
    elif luck >= 70:
        st.info("😊 好運加持!值得一試!")
    else:
        st.info("🍀 穩定運勢,機會就在眼前!")
    
    st.markdown("---")
    st.markdown("### 🎉 祝您中大獎!!")

# =====================================================
# 頁面底部說明
# =====================================================
st.markdown("---")
with st.expander("ℹ️ 使用說明"):
    st.markdown("""
    ### 統計理工模式 🧠
    - 根據歷史開獎記錄,分析號碼出現頻率和配對關係
    - 可調整參數來改變選號策略
    - 可選擇使用的歷史資料期數(近1/3/5年或全部)
    - 適合喜歡數據分析的玩家
    
    ### 天選之人模式 🔮
    - 完全隨機選號,不受歷史影響
    - 每個號碼機率完全相同
    - 適合相信命運和直覺的玩家
    
    ### 關於幸運指數 ✨
    - 幸運指數是純粹的「運氣加持」,兩種模式都是隨機生成
    - 分數越高代表這組號碼越幸運(僅供娛樂參考)
    - **重要提醒:幸運指數與實際中獎機率無關!**
    
    ### 歷史資料建議 📊
    - **近3年**(推薦): 平衡時效性與統計穩定性
    - **近5年**: 更多樣本,統計結果更可靠
    - **全部資料**: 包含所有歷史記錄,但較舊的資料參考價值較低
    
    ### 溫馨提醒 💝
    彩券是娛樂性質,請理性購買,量力而為
    """)
